# Copiar a .env en la raíz (solo Docker) o usar los mismos valores en backend/.env

POSTGRES_USER=erp_dev
POSTGRES_PASSWORD=cambiar-password-local
POSTGRES_DB=ERPTienda
# En Fedora suele haber PostgreSQL en 5432; usar 5433 para el contenedor ERP:
POSTGRES_PORT=5433
