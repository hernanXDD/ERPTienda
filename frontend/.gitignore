# Hereda reglas del .gitignore en la raíz del monorepo.
# Entradas locales por si se trabaja solo dentro de frontend/.

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!.env.production.example
coverage/
.vitest/
*.log
.vscode/
