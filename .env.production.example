# Copiar a .env.production (raíz) — solo PostgreSQL en Docker.
# La API usa backend/.env con DATABASE_URL apuntando a 127.0.0.1:5432.
# No commitear .env.production — contiene contraseñas.

POSTGRES_USER=erp
POSTGRES_PASSWORD=generar-password-fuerte-y-unico
POSTGRES_DB=ERPTienda
